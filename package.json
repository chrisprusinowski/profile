{
  "name": "profile-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "dev:api": "npm run dev --workspace=api",
    "dev:client": "npm run dev --workspace=client",
    "dev:postgres": "docker compose up -d postgres",
    "db:migrate": "npm run db:migrate:local",
    "dev:fullstack": "npm run dev:postgres && npm run db:migrate && (npm run dev:api & npm run dev:client)",
    "db:migrate:docker": "docker compose run --rm -e DATABASE_URL=postgresql://app_user:app_password@postgres:5432/app_db api npm run db:migrate",
    "db:migrate:local": "node scripts/migrate.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.12.0",
    "@types/node": "^22.7.4",
    "eslint": "^9.12.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.2",
    "typescript-eslint": "^8.8.0"
  },
  "type": "module"
}
