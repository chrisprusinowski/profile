{
  "name": "profile-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present --parallel",
    "start": "npm run start --workspaces --if-present --parallel",
    "build": "npm run build -w @profile/shared && npm run build -w @profile/api && npm run build -w @profile/web",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run build -w @profile/shared && npm run typecheck -w @profile/api && npm run typecheck -w @profile/web && npm run typecheck -w @profile/shared",
    "test": "npm run test --workspaces --if-present"
  },
  "packageManager": "npm@10.9.2"
}
